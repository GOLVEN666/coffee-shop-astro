<!-- ---
export interface Props {
  src: string;
  alt: string;
  className?: string;
}

const { src, alt, className = "coffeeimg" } = Astro.props;
---

<img class={className} src={src} alt={alt} /> -->

<!-- This is just a test -->