import { defineConfig } from 'astro/config';

// https://astro.build/config


export default defineConfig({
  devOptions: {
    hostname: '8.4.8.4', // Use '0.0.0.0' to listen on all network interfaces
  },
});